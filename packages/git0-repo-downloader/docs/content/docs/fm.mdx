---
title: fm
description: Terminal file manager bundled with git0.
---

`fm` is a keyboard-driven terminal file manager that ships as one of git0's bins.
It is not built — the bin points straight at `src/fm.js`.

```bash
fm
```
